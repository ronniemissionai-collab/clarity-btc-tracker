{
  "name": "@clarity-btc/site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static front-end - approved Variant D composite (hero, stage strip, tabs, party columns, trader cards)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p . && vite build"
  },
  "dependencies": {
    "@clarity-btc/shared": "*"
  },
  "devDependencies": {
    "happy-dom": "^20.0.0"
  }
}
